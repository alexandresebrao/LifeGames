<div class="col-lg-2">
	<%= link_to image_tag(product.coverbox.thumb), view_product_path(product) %>
	<%= link_to product.name, view_product_path(product) %>
	<br><%if product.discount_percentage > 0%><span style:"text-decoration: line-through"><strike>R$<%= format_number product.price %></strike></span> Desconto (<%= product.discount_percentage %>%) <br><b>R$<%= product.final_valor%></b><%else%>$<%= product.price %><%end%>
</div>
